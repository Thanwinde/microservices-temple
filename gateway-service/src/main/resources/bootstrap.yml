spring:
  application:
    name: gateway # 服务名称
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.247.133:8848 # nacos地址
      config:
        group: DEFAULT_GROUP
        file-extension: yml # 文件后缀名
        shared-configs: # 共享配置
          - dataId: private-route.yml
          - dataId: shared-log.yml